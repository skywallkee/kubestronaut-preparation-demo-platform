{
  "id": "ckad-i-192",
  "title": "Create an nginx pod that mounts the secret mysecret2 in a volume on path /etc/foo",
  "description": "Create an nginx pod that mounts the secret mysecret2 in a volume on path /etc/foo",
  "difficulty": "intermediate",
  "category": "Configuration",
  "tags": [
    "default",
    "secrets",
    "pods"
  ],
  "points": 6,
  "timeLimit": 10,
  "infrastructure": {
    "namespaces": [
      "default"
    ],
    "resources": [
      "pods",
      "secrets"
    ],
    "prerequisites": []
  },
  "solution": {
    "steps": [
      "1. kubectl run nginx --image=nginx --restart=Never -o yaml --dry-run=client > pod.yaml",
      "2. vi pod.yaml",
      "3. kubectl create -f pod.yaml",
      "4. kubectl exec -it nginx -- /bin/bash",
      "5. ls /etc/foo  # shows username",
      "6. cat /etc/foo/username # shows admin"
    ]
  },
  "validations": [
    {
      "command": "echo OK",
      "expected": "OK",
      "points": 1,
      "description": "Placeholder validation"
    }
  ]
}