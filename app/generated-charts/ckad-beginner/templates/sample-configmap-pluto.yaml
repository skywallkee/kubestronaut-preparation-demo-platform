apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-config-pluto
  namespace: pluto
  labels:
    exam-resource: "true"
    planet: pluto
data:
  app.properties: |
    debug=false
    database.host=localhost
    database.port=5432
    planet=pluto
  config.yaml: |
    server:
      port: 8080
      timeout: 30s
      namespace: pluto