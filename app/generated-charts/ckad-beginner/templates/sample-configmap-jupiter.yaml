apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-config-jupiter
  namespace: jupiter
  labels:
    exam-resource: "true"
    planet: jupiter
data:
  app.properties: |
    debug=false
    database.host=localhost
    database.port=5432
    planet=jupiter
  config.yaml: |
    server:
      port: 8080
      timeout: 30s
      namespace: jupiter