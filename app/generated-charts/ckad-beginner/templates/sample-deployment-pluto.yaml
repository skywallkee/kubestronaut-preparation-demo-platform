apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-deployment-pluto
  namespace: pluto
  labels:
    app: sample-app-pluto
    exam-resource: "true"
    planet: pluto
    type: "sample"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-app-pluto
  template:
    metadata:
      labels:
        app: sample-app-pluto
        planet: pluto
    spec:
      containers:
      - name: nginx
        image: nginx:1.20
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"