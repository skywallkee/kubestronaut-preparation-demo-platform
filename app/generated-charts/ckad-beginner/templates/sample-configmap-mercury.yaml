apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-config-mercury
  namespace: mercury
  labels:
    exam-resource: "true"
    planet: mercury
data:
  app.properties: |
    debug=false
    database.host=localhost
    database.port=5432
    planet=mercury
  config.yaml: |
    server:
      port: 8080
      timeout: 30s
      namespace: mercury