apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-config-venus
  namespace: venus
  labels:
    exam-resource: "true"
    planet: venus
data:
  app.properties: |
    debug=false
    database.host=localhost
    database.port=5432
    planet=venus
  config.yaml: |
    server:
      port: 8080
      timeout: 30s
      namespace: venus