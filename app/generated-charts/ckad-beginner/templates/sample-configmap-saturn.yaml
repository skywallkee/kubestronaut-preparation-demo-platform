apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-config-saturn
  namespace: saturn
  labels:
    exam-resource: "true"
    planet: saturn
data:
  app.properties: |
    debug=false
    database.host=localhost
    database.port=5432
    planet=saturn
  config.yaml: |
    server:
      port: 8080
      timeout: 30s
      namespace: saturn